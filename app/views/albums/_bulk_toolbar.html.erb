<%= form_with url: album_bulk_actions_path,
      method: :post,
      html: {
        id: form_id,
        class: "hidden items-center gap-2",
        data: { bulk_selection_target: "bar", action: "submit->bulk-selection#guard" }
      } do %>
  <p class="hidden rounded-lg bg-zinc-950 px-3 py-2 text-sm font-semibold text-white shadow-sm sm:block" aria-live="polite" data-bulk-selection-target="count">
    Select albums, then choose an action.
  </p>

  <div class="flex items-center gap-1 rounded-lg border border-zinc-200 bg-white p-1 shadow-sm">
      <%= bulk_action_button(icon: :x, label: "Deselect all", type: "button", action: "bulk-selection#clear") %>
      <%= bulk_action_button(icon: :globe, label: "Publish selected albums", value: "publish") %>
      <%= bulk_action_button(icon: :lock, label: "Make selected albums private", value: "unpublish") %>

      <%= render "shared/bulk_confirm_modal",
            button_label: "Remove selected albums",
            title_id: "remove-selected-albums-title",
            title: "Remove selected albums?",
            body: "This removes the album containers. Photos stay in the stream.",
            value: "delete",
            confirm_label: "Remove" %>
  </div>
<% end %>
