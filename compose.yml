services:
  db:
    image: postgres:18
    restart: unless-stopped
    env_file:
      - .env.postgres
    environment:
      POSTGRES_DB: photos_production
      POSTGRES_USER: photos
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U photos -d photos_production"]
      interval: 30s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_data:/var/lib/postgresql

  web:
    build: .
    image: photos:production
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      RAILS_ENV: production
      PGHOST: db
      PGUSER: photos
      SOLID_QUEUE_IN_PUMA: "false"
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "3000:80"
    volumes:
      - ${PHOTOS_STORAGE_PATH:-app_storage}:/rails/storage

  worker:
    image: photos:production
    restart: unless-stopped
    command: ["./bin/jobs"]
    env_file:
      - .env.production
    environment:
      RAILS_ENV: production
      PGHOST: db
      PGUSER: photos
      SOLID_QUEUE_IN_PUMA: "false"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ${PHOTOS_STORAGE_PATH:-app_storage}:/rails/storage

volumes:
  postgres_data:
  app_storage:
